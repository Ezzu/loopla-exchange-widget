# Production Environment Variables (for Railway Frontend Service)

# Environment
NODE_ENV=production
BUILD_TARGET=production

# Backend API - Set to your backend Railway URL
VITE_API_BASE_URL=https://your-backend.railway.app

# Port (nginx serves on port 80)
PORT=80
